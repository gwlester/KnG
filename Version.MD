# Current Version

`0.1.0`

Baseline pre-release version for the KnG Consulting site. Release channel and
attempt numbers (`vX.Y.Z-a.N` / `vX.Y.Z-b.N`) are assigned by Git tags rather
than stored in this base version. See `CLAUDE.md` for the release workflow.
