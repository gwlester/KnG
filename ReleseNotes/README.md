# Release Notes

Release notes live here as `<exact-tag>.md` files, one per published release
tag — see the top-level `CLAUDE.md` for the full release workflow and
approval gate.

No releases have been tagged yet.
